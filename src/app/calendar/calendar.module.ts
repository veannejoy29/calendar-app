// calendar.module.ts
import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { CalendarComponent } from './calendar.component';

@NgModule({
  // declarations: [
  //   CalendarComponent // Declare the app-calendar component
  // ],
  imports: [
    CommonModule
  ],
  // exports: [
  //   CalendarComponent // Export the app-calendar component if needed in other modules
  // ]
})
export class CalendarModule { }
